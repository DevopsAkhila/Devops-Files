//Docker Dynamic agent - Maven
pipeline{
agent none
 //agent{
 //use specific version of maven inside docker container compatible with jdk17  with volume mapping and running as root user
        //docker{
           // image 'maven:3.8.3-openjdk-17'
           // args '-u root -v /root/.m2:/root/.m2' 
       // }
    //}

     //agent{
 //use 'maven' image with volume mapping and running as root user
        //docker{
           // image 'maven'
           // args '-u root -v /root/.m2:/root/.m2' 
       // }
    //}

    //Image name is taken from the dockhub repository

        stages{
             stage ('Compile the code')
            {
                agent{
        docker{
            image 'maven:3.8.3-openjdk-17'
            args '-u root -v /root/.m2:/root/.m2' 
        }
    }
                steps{
                   echo "Compile code"
                   git 'https://github.com/Sonal0409/DevOpsCodeDemo.git'
                   sh 'mvn compile'
                }
            }
            stage ('Test the code')
            {agent{
        docker{
            image 'maven:3.8.3-openjdk-17'
            args '-u root -v /root/.m2:/root/.m2' 
        }
    }
                steps{
                     echo "Testing the code"
                     git 'https://github.com/Sonal0409/DevOpsCodeDemo.git'
                   sh 'mvn test'
                }
            }
    }
}
