# Jenkins Agents and Docker Pipeline Lab

## Objectives
- Run builds on static Jenkins agents
- Use Docker-based dynamic agents
- Cache dependencies
- Build Java and Python applications

## Requirements
- Jenkins controller running
- Docker installed on controller
- Static Jenkins agent configured
- Docker Pipeline plugin installed

## How to Run
1. Create Jenkins Pipeline job
2. Point to this Git repository
3. Run build and observe agent allocation
